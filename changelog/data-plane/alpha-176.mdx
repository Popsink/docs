---
title: "v0.1.0-alpha.176 11/09/2026"
---

---

**Description**: [DOCSLIM] Compact Args/Returns boilerplate in domains/ports.

### Changes

- [DOCSLIM] Compact Args/Returns boilerplate in domains/ports (#3957).
- [TESTCLEAN] Collect the two test classes pytest was silently skipping (#3955).
- DP - Pin what a successful environment lazy load writes locally (#3953).
- [SEC] Authorize the object a Kafka message preview reads (#3920 part 2) (#3952).
- [FRONT] Extract the connector detail tabs into useConnectorDetailTabs (#3951).
- DP - Delete the dead admin domain service and ratchet the domains/ dependency rule (#3944).
- A backfill can no longer report tables done that never ran (#3937).
- DP - Deleting a subscription can drop what Popsink provisioned in the target (#3915).
- DP - Keyless JDBC targets, and APPLY says why it is disabled (#3916).
- IBM i: refuse a multi-journal table selection in the picker and the API (#3880).
- DP - SQL Server without CDC tables can now be a source (beta) (#3989).
- Bump snow_target past develop's 2026.09.09 so #3833 ships (#4002).
- DP - A new connector loads history through the backfill, not a source snapshot (#3994).
- DP - A Snowflake column named ORDER no longer kills its table forever (#4009).
- Refuse a mapper primary key that narrows the declared one (#3990) (#4005).
- Compact Args/Returns boilerplate in domains/repositories (#4025).
- Assert the agreement the option tests only described (#4024).
- Lock POST /auth/verify — audience separation and the replay guard (#4023).
- Extract the datamodel detail page's server state into useDatamodelDetail (#4020).
- DP - 17 use cases stop holding a database session they never use (#4019).
- Gate GET /users on superuser (Part 3 of #3920) (#4021).
- UC worker - hold a capacity refusal instead of dropping it, and report the lag (#4010).
- Direct data-plane URL loops forever between the data plane and the control plane: the SSO return trip carries no token (#4027).
- DP - Report a Snowflake merge key that does not identify a row, and refuse one that does not exist (#4007).
- DP - A backfill's rows come from its Job, and its silence is reported (#4017).
- Worker - An IBM i source that stops freezing, and catches up (#4034).
